import '../../../../config.dart';

class TableRowCommon extends StatelessWidget {
  const TableRowCommon({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
